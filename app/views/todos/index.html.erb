<ul class="todos">
  <% @todos.each do |t| %>
    <li class='<%= "completed" if t.completed? %>'>
      <%= t.title %>
      <%= link_to "Complete", todo_completion_path(t), method: :post %>
    </li>
  <% end %>
</ul>

<%= link_to "Create a todo", new_todo_path, rel: 'create-todo'%>
